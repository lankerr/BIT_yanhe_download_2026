# BIT_yanhe_download 2026

> **2026增强版** - 64线程并发下载 + Watchdog死锁监控 + VPN友好 + 一键exe
> **原版痛点**: 单线程慢 · 不支持VPN · 容易卡死 · 操作繁琐
> **2026解决**: 多线程快 · 代理友好 · 自动恢复 · 极简操作

---

## 为什么要用2026版？

| 对比项 | 原版 | 2026版 |
|--------|------|--------|
| 下载速度 | 1小时/视频 | **2分钟/视频** (30倍提升) |
| VPN支持 | ❌ 开启代理报错 | ✅ 智能检测 + 白名单提示 |
| 卡死恢复 | ❌ 手动重启 | ✅ Watchdog自动恢复 |
| 使用难度 | ❌ 4个exe，操作繁琐 | ✅ 单一exe，双击即用 |

---

## 原版介绍

本项目可下载[延河课堂 (yanhekt.cn)](https://www.yanhekt.cn/recordCourse)中的课程视频。延河课堂是北京理工大学的在线课堂，提供了大量的课程视频，但是没有提供下载功能。本项目可以下载指定课程的摄像头和屏幕信号，包括无权限的课程。

- **原项目**: [AuYang261/BIT_yanhe_download](https://github.com/AuYang261/BIT_yanhe_download)
- **2026版本**: [lankerr/BIT_yanhe_download_2026](https://github.com/lankerr/BIT_yanhe_download_2026)

欢迎提出建议和 star ！

---

## 原版存在的严重问题

| 问题 | 原版表现 | 2026版解决方案 |
|------|----------|----------------|
| **单线程下载** | 1小时下载一个视频 | **64线程并发**，2分钟搞定！** |
| **不支持VPN** | 开启代理直接报错无法使用 | **智能代理检测**，友好提示白名单 |
| **无Watchdog监控** | 任务卡死必须手动重启 | **120秒超时检测**，自动恢复 |
| **下载容易中断** | 网络波动就失败 | **指数退避重试** + 线程自适应 |
| **WebUI臃肿** | 需启动Flask服务器，占用资源 | **原生GUI**，轻量流畅 |
| **操作繁琐** | 需要4个不同的exe程序 | **单一exe**，双击即用 |

> **实测性能**: 2小时视频从 **1小时** 缩短到 **2分钟** 下载完成！
| **不支持VPN** | 开启代理直接报错无法使用 | **智能代理检测**，友好提示白名单 |
| **无Watchdog监控** | 任务卡死必须手动重启 | **120秒超时检测**，自动恢复 |
| **下载容易中断** | 网络波动就失败 | **指数退避重试** + 线程自适应 |
| **WebUI臃肿** | 需启动Flask服务器，占用资源 | **原生GUI**，轻量流畅 |
| **操作繁琐** | 需要4个不同的exe程序 | **单一exe**，双击即用 |

---

## 2026版本改进

### 1. 多线程自适应下载引擎

| 特性 | 原版 | 2026版 |
|------|------|--------|
| 线程策略 | 固定线程数 | 慢启动(16) + 动态调整(1-64) |
| 失败重试 | 立即重试 | 指数退避 + 线程减半 |
| 内存管理 | 无界队列 | 有界队列(2x线程数) |
| 超时处理 | 固定超时 | 自适应超时(40-120s) |

### 2. Watchdog 死锁监控

- 自动检测卡死的下载任务（120秒无活动）
- 自动重启卡死的任务
- 避免单个任务阻塞整体进度

### 3. 现代化GUI（极客黑风格）

- **GitHub Dark 配色**: #0d1117 背景 + #58a6ff 强调色
- **并发控制**: 滑块调整 4-64 线程
- **实时进度**: 每任务进度条 + active/max 线程显示
- **便捷操作**: 打开输出目录、重试失败任务

### 4. 独立exe程序

- 无需安装Python环境
- 双击即用
- 文件大小约30MB

### 5. VPN/代理友好

- 智能检测代理冲突
- 清晰的错误提示
- 支持添加白名单

---

## 即将推出

以下功能正在开发中，敬请期待：

| 功能 | 状态 | 说明 |
|------|------|------|
| **自动转录字幕** | 开发中 | 基于Whisper的语音转文字，支持中英混合 |
| **代码提取器** | 开发中 | 自动识别并提取视频中的代码片段 |

---

## 快速开始

### 下载exe（推荐）

直接下载 [Release 1.0](https://github.com/lankerr/BIT_yanhe_download_2026/releases/latest) 中的 `延河课堂下载器.exe`

1. 双击运行 `延河课堂下载器.exe`
2. 输入课程ID（5位数字）
3. 输入Token（首次需要）
4. 选择视频并开始下载

### 获取Token步骤

1. 在浏览器打开 https://www.yanhekt.cn 并登录
2. 按 F12 打开开发者工具，切换到「控制台」标签
3. 在控制台输入以下代码并按回车：
   ```
   javascript:alert(JSON.parse(localStorage.auth).token)
   ```
   或者直接输入：`JSON.parse(localStorage.auth).token`
4. 复制弹出的认证码（32位字符）粘贴到输入框

---

## 使用说明

### 1. 获取课程ID

在[延河课堂](https://www.yanhekt.cn/recordCourse)找到想下载的课程，复制链接末尾的**5位数字**。

例如: `https://www.yanhekt.cn/course/40524` → 课程ID是 **40524**

**注意**: 是课程列表链接(`yanhekt.cn/course/五位编号`)，不是视频界面链接(`yanhekt.cn/session/六位编号`)

---

## 打包（开发者）

```bash
# 一键打包
build_exe.bat

# 或手动打包
python -m PyInstaller --name="延河课堂下载器" --onefile --windowed --noconfirm --clean --hidden-import=customtkinter --collect-all=customtkinter gui_app.py
```

---

## 文件说明

```
yanhedown/
├── gui_app.py          # GUI主程序
├── m3u8dl.py           # 多线程下载引擎
├── utils.py            # 工具函数
├── main.py             # 命令行入口
├── requirements.txt    # Python依赖
└── build_exe.bat       # 一键打包脚本
```

---

## 系统要求

- **Windows 10/11**
- **Python 3.9+** （仅源码运行需要）
- 无需安装FFmpeg（已内置）

---

## 注意事项

1. **VPN/代理**: 程序会智能检测代理冲突，如遇报错请将 `yanhekt.cn` 加入代理白名单
2. Token有效期约24小时，过期需重新获取
3. 下载的视频保存在 `output/` 目录
4. 支持断点续传

---

## 版本历史

| 版本 | 主要改进 |
|------|----------|
| **2026 v1.0** | 64线程并发 · Watchdog死锁监控 · VPN友好 · 现代化GUI · 单一exe |
| **v2.0 (原版)** | WebUI · 字幕生成 · 单线程下载 · 不支持代理 |

---

## 许可证

MIT License

---

## 致谢

- 原项目 [AuYang261/BIT_yanhe_download](https://github.com/AuYang261/BIT_yanhe_download)
- CustomTkinter - 现代化UI框架
